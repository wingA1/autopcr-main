# AutoPCR 本地启动指南

## 环境要求

| 组件 | 说明 |
|------|------|
| Python | conda 环境 `autopcr` |
| Node.js | 已安装 npm |

## 目录结构

| 路径 | 用途 |
|------|------|
| `autopcr-main` | 项目根目录（后端） |
| `autopcr-main/AutoPCR_Web` | 前端项目 |
| `autopcr-main/cache` | 缓存（用户配置、session） |
| `autopcr-main/log` | 日志 |
| `autopcr-main/result` | 运行结果 |

## 启动步骤

### 1. 启动后端

在项目根目录打开终端，执行：

```powershell
conda activate autopcr
python _httpserver_test.py
```

后端默认监听 `0.0.0.0:13200`，启动成功后终端会持续运行，不要关闭。

### 2. 启动前端

另开一个终端，在前端目录执行：

```powershell
cd AutoPCR_Web
npm run dev
```

Vite 开发服务器默认在 `http://localhost:5173` 启动，已配置代理将 `/daily/api` 请求转发到后端 `http://localhost:13200`。

### 3. 访问

浏览器打开：

```
http://localhost:5173/daily
```

## 关闭服务

- 后端：在后端终端按 `Ctrl+C`
- 前端：在前端终端按 `Ctrl+C`

## 首次部署额外步骤

如果从未下载过前端静态资源和游戏数据，需要先执行：

```powershell
conda activate autopcr
python _download_web.py    # 下载前端静态资源（也可传入本地 zip 路径）
python _download_data.py   # 下载数据库和图片资源（可选）
```

## 常见问题

### 端口被占用

后端默认端口 `13200`，可通过环境变量修改：

```powershell
$env:AUTOPCR_SERVER_PORT = `13201`
```

前端 Vite 端口如被占用会自动递增。

### 依赖缺失

后端依赖：

```powershell
conda activate autopcr
pip install -r requirements.txt
```

前端依赖：

```powershell
cd AutoPCR_Web
npm install
```